/// @description Takes Player to assigned Camera in the Grima Overlay

// Only want this to work if the Overlay is showing
if ( obj_grima_controller.show_overlay ) {
	
	obj_grima_controller.curr_camera = camera
	
}
